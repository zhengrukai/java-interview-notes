## StringBuffer：线程安全

它的所有核心方法（如 append()、insert()）都加了 synchronized 关键字（同步锁），保证多线程同时操作时，不会出现数据错乱（比如拼接内容丢失、重复）。

## StringBuilder：非线程安全

方法没有加 synchronized 锁，多线程同时修改同一个 StringBuilder 对象时，可能出现数据不一致的问题，但单线程下完全没问题。
